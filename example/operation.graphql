query ExampleQuery($characterId: ID!, $includeName: Boolean = true) {
  character(id: $characterId) {
    id
    name @include(if: $includeName)
    ... @defer {
      episode {
        id
        name
      }
    }
  }
}
